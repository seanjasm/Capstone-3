
@if (@Session["UserName"] != null)
{
    <ul class="nav navbar-nav navbar-right">
        <li>
            @Html.ActionLink("Hi, " + @Session["UserName"] + "!", "Login", "Users")            
        </li>
        <li>
            @Html.ActionLink("Log out", "Logout", "Members")
        </li>
    </ul>

}
else
{
    <ul class="nav navbar-nav navbar-right">
        <li>@Html.ActionLink("Register", "Register", "Members")</li>
        <li>@Html.ActionLink("Log in", "Login", "Members")</li>
    </ul>
}